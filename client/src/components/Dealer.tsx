import React, { ReactElement } from 'react';
import { GameDealerType } from '../types/gameType/gameFromServerType';

export default function Dealer (props:GameDealerType):ReactElement{
    const {cards, name, total} = props;

    return <div></div>
}